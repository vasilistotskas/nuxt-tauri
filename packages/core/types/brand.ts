export interface BrandConfig {
  name: string
  author: string
  colors: {
    primary: string
    primaryDark: string
  }
  logo: string
  metadata: {
    title: string
    description: string
  }
}
